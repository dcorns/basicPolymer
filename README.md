#Add a Polymer Element
###A simple example of including a polymer element on a web page
Assuming you have node installed perform the following:
npm install -g bower
bower init
bower install Polymer/polymer

bower install polymerelements/iron-elements --save
bower install polymerelements/paper-elements --save

Once done with the installs you can open the index page to see the results.